<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.iastate.bitfitx">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.WeightGraphActivity"></activity>
        <activity android:name=".Activities.WorkoutHistoryActivity" />
        <activity android:name=".Activities.CommunityListActivity" />
        <activity android:name=".Activities.TrackWorkoutActivity" android:configChanges="orientation|screenSize"/>
        <activity android:name=".Activities.SettingsActivity" />
        <activity android:name=".Activities.LogWorkoutActivity" />
        <activity android:name=".Activities.DashboardActivity" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity
            android:name=".Activities.DatabaseTestActivity"
            android:label="@string/title_activity_database_test"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>